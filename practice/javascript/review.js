// Storing things in javascript.

var nameOfVar = 1000;

// Javascript DataTypes

// String, ALWAYS wrapped in single or double matching quotes.
"Hello there!";

// Number
2910.80234
12399
-32003; -1 * 32003;

// Boolean
true;
false;

// Do not use keywords to name things
// var false = "hi";
// Do not start with #
// var 10Hello = 1000;
// Recommended no start with caps.
var Capital = "hello";
// No spaces in naming.
//var first word = "hello";

//Camel Case
var helloThereHowAreYou = 100;

//Snake Case
var hello_there_how_are_you = 40;

// Operators

//Math
// + adds, concatination
// - substraction
// * multiplication
// / division
// % modulo

// Comparators
// == Is the same as
// > Greater than
// < Lesser than
// >= Greater or equal
// <= Lesser or equal
// != not equal

//Boolean Operator
// ! Opposite

// Falsey values.
undefined(a avariable with no defined value);
null(a single null value);
naming(boolean true or false);
0 (infinity and NaN);
""(empty string);
Symbol

// Truthy values
all values are truthy unless they are defined as falsey.

//Structures

// If statement

if (/* condition */ false) {
    /* code to run if its true. */
} else {
    /* code to run if its false. */
}

// Loops
//for ( initialization; condition; iteration) {}

//Function
function nameOfFunction(/* parameters */) {
    /* code block */
}